// Variables and Data Types
var firstName = "Peter";
var birthYear = 2013;
var isAdult = false;
var job;
var favMovie = null;

// Type Coercion
var result = firstName + birthYear;
console.log(`${result} is ${typeof result}`);
